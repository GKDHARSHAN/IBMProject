pipeline { 
    agent any
    environment {
        SLACK_CHANNEL = '#general'  
        SLACK_COLOR_SUCCESS = 'good'        
        SLACK_COLOR_FAILURE = 'danger'
        JIRA_SITE = 'jenkins-jira'
        JIRA_ISSUE = 'SMS-7' // Replace with the actual Jira issue ID
    }
script {
    echo "JIRA Issue Key: ${JIRA_ISSUE}"
}
    stages {
        stage('Build') {
            steps {
                script {
                    echo 'Building the project...'
                    // Uncomment below to fail the build intentionally
                    // error 'Build failed intentionally!'
                }
            }
        }
        stage('Test') {
            steps {
                script {
                    echo 'Running tests...'
                    // Add test steps here
                }
            }
        }
    }
    post {
        success {
            script {
                slackSend (
                    channel: SLACK_CHANNEL, 
                    color: SLACK_COLOR_SUCCESS, 
                    message: "✅ Build successful! Build ID: ${env.BUILD_NUMBER}",
                    username: 'jenkins',
                    iconEmoji: ':tada:'
                )
                jiraAddComment(issueKey: JIRA_ISSUE, comment: "✅ Build Successful! Build ID: ${env.BUILD_NUMBER}")
                jiraTransitionIssue(issueKey: JIRA_ISSUE, transition: "Done") // Moves issue to "Done"
            }
        }
        failure {
            script {
                slackSend (
                    channel: SLACK_CHANNEL, 
                    color: SLACK_COLOR_FAILURE, 
                    message: "❌ Build failed! Build ID: ${env.BUILD_NUMBER}",
                    username: 'jenkins',
                    iconEmoji: ':no_entry_sign:'
                )
                jiraAddComment(issueKey: JIRA_ISSUE, comment: "❌ Build Failed! Build ID: ${env.BUILD_NUMBER}")
                jiraTransitionIssue(issueKey: JIRA_ISSUE, transition: "Reopen") // Moves issue back to "Reopen"
            }
        }
    }
}
